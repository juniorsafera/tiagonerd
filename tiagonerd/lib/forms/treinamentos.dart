import 'package:flutter/material.dart';

class FormTreinamentos extends StatelessWidget {
  const FormTreinamentos({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}